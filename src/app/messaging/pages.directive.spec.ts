import { PagesDirective } from './pages.directive';

describe('PagesDirective', () => {
  it('should create an instance', () => {
    const directive = new PagesDirective();
    expect(directive).toBeTruthy();
  });
});
